:root {
  --color-beige: #F2E2C9;
  --color-yellow: #FBB108;
  --color-orange: #FE4B01;
  --color-pink: #FF9B9E;
}


/* General styles */
body {
  margin: 0;
  font-family: Arial, sans-serif;
}

/* Header styling */
header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15px;
  background-color: #F2E2C9; 
}

/* Logo styling */
.logo {
  font-size: 24px;
  font-weight: bold;
  color: #FE4B01; 
}

/* Burger menu styling */
.burger-menu {
  cursor: pointer;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  width: 30px;
  height: 21px;
}

.burger-menu .bar {
  height: 3px;
  background-color: #FBB108; 
  border-radius: 5px;
}

/* Hidden nav menu */
.hidden {
  display: none;
}

/* Visible nav menu (when active) */
nav ul {
  list-style: none;
  padding: 0;
  margin: 0;
  background-color: #FF9B9E; 
  position: absolute;
  top: 60px;
  right: 15px;
  width: 150px;
}

nav ul li {
  text-align: center;
  padding: 10px;
}

nav ul li a {
  text-decoration: none;
  color: #000;
  font-size: 18px;
}
